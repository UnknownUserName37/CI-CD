name: CI for CI/CD Project

on :
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
    hello-world:
      runs-on: ubuntu-latest

      steps:
      - name: Checkout code
      - uses: actions/checkout@v3

      - name: Say Hallo
      - run: echo "Hello, GitHub Actions! Im really VERY HAPPY! This is my first workflow!

      - name: Show date
        run: date
